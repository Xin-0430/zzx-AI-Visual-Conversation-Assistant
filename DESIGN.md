# AI 视觉对话助手 --- 设计文档

## 一、项目概述

项目名称: AI Visual Conversation Assistant (AI-VCA)
开发周期: 3 天
目标: 构建一款能够通过摄像头看到视频内容、通过麦克风听到用户语音、并给予恰当视觉理解回应的 AI 对话应用。
技术栈: 原生 JavaScript + CSS3 + HTML5 + Web Speech API + 可选 OpenAI/Gemini API

## 二、用户故事: 计划与实现对比

### 计划实现的用户故事

1. 打开摄像头让 AI 看到实时画面 [P0] [已实现]
2. 语音提问，AI 识别语音 [P0] [已实现]
3. AI 语音回答 [P0] [已实现]
4. 文字输入问题 [P0] [已实现]
5. 视障辅助: 描述环境+障碍物提示 [P0] [已实现]
6. 儿童教育: 动物/颜色/字母识别 [P0] [已实现]
7. 老人陪伴: 药品/保质期识别 [P0] [已实现]
8. 懒人模式: 语音直接让 AI 帮看 [P0] [已实现]
9. 控制帧率和图片质量 [P1] [已实现]
10. 查看 Token 消耗和费用 [P1] [已实现]
11. 切换 AI 服务商 [P1] [已实现]
12. 开关语音回放 [P1] [已实现]

### 额外实现

- 专属系统提示词(每个场景 300-500 字精细调校)
- 离线模拟模式(无需 API Key)
- 成本追踪(请求次数+Token+费用)
- 自动初始分析(开摄像头后自动触发)
- 配置本地持久化(localStorage)
- 响应式设计(桌面+移动端)
- 自定义提示词覆盖

## 三、架构设计

### 系统架构

Browser 前端层:
- Camera.js: getUserMedia 摄像头管理、帧捕获、压缩
- Speech.js: Web Speech API 语音识别 + 合成
- Scenes.js: 四种场景模式的提示词配置
- App.js: 主控制器，串接各模块

AI Service 抽象层:
- AIProvider 接口: analyze(image, text, systemPrompt)
- MockProvider: 离线模拟
- OpenAIProvider: GPT-4o/GPT-4o-mini 视觉API
- GeminiProvider: Gemini 2.0 Flash 视觉API
- CostTracker: 追踪Token和费用

### 交互流程

用户说话/打字 → App.js接收 → Camera.js捕获帧(采样+压缩) → AIService.analyze(image+text+systemPrompt) → AI Provider处理 → 返回响应+成本 → 聊天区显示+语音播报

### 关键技术决策

1. Vanilla JS: 减少依赖，3天开发更高效
2. AI Provider抽象: 可插拔后端，统一接口
3. Web Speech API(非云语音): 免费、零延迟、隐私保护

## 四、运营成本控制策略

### 思考过的技巧

已采用:
- 帧采样间隔(默认3帧,降67%调用量)
- JPEG压缩(30-90%,降90%传输量)
- 分辨率限制(320/640/1280px)
- 模型选型(gpt-4o-mini,便宜16x)
- detail:low(按85x85 tile计费)
- isProcessing防重复锁
- 数据量可视化引导

未采用:
- API缓存(牺牲实时性)
- 流式响应(增加复杂度)
- 本地OCR(Tesseract精度不够)
- 批处理(影响交互体验)
- 云端语音(Web Speech免费)
- 模型Fallback(增加复杂度)

### 成本估算 (gpt-4o-mini, 640px, detail:low, 3帧采样)

低频: ~$0.0003/图, 60次/时, ~$0.36/月
中度: ~$0.0003/图, 300次/时, ~$1.80/月
高频: ~$0.0003/图, 1000次/时, ~$6.00/月

## 五、项目文件

D:\ai-visual-assistant\
  index.html - 主页面
  css/style.css - 全局样式
  js/camera.js - 摄像头模块
  js/speech.js - 语音模块
  js/ai-service.js - AI 服务抽象层
  js/scenes.js - 场景模式配置
  js/app.js - 主控制器
  DESIGN.md - 设计文档
  README.md - 项目说明

## 六、后续改进

1. 端侧模型: WebLLM/MediaPipe 脱网运行
2. 连续视频流: MediaRecorder 实时流
3. 视觉记忆: 跨帧理解上下文
4. 本地OCR: Tesseract.js
5. 移动端触摸优化
6. 无障碍: 屏幕阅读器+大字体
7. 音频录制: Whisper补充兼容性
# 📄 AI Visual Conversation Assistant（AI-VCA）设计文档

---

# 1. 📌 产品概述（Product Overview）

## 1.1 产品名称

AI Visual Conversation Assistant（AI-VCA）

---

## 1.2 产品定位（Product Positioning）

AI-VCA 是一款**基于浏览器的多模态 AI 视觉对话助手**，通过整合：

> 👁 摄像头视觉理解 + 🎤 语音交互 + 🧠 大模型推理能力

实现"可感知现实环境的 AI 对话体验"。

---

## 1.3 产品愿景（Vision）

构建一个轻量级、多模态、低成本的 AI 助手，使用户可以：

> "像与真人助手交流一样，让 AI 看到、听到并理解现实世界。"

---

## 1.4 目标用户（Target Users）

* 视障辅助用户
* 儿童教育场景用户
* 老人生活辅助用户
* 日常"懒人信息查询"用户
* AI 技术体验用户

---

## 1.5 产品价值（Core Value）

* 降低人与环境信息获取成本
* 提供实时视觉理解能力
* 提供自然语音交互体验
* 实现低成本多模态 AI 应用

---

# 2. 👤 用户故事（User Stories）

---

## 2.1 产品级用户故事设计（To-Be）

### 🎯 P0 核心闭环能力

| 用户故事  | 描述                 |
| ----- | ------------------ |
| 视觉理解  | 用户打开摄像头，AI 能理解当前环境 |
| 语音交互  | 用户可通过语音与 AI 自然对话   |
| 多模态融合 | AI 同时结合图像 + 语音进行判断 |
| 实时反馈  | AI 能快速返回可理解的自然语言结果 |

---

### 🎯 P1 场景化能力

| 场景   | 用户故事                |
| ---- | ------------------- |
| 视障辅助 | AI 描述环境并提示潜在障碍      |
| 儿童教育 | AI 识别物体并进行教学讲解      |
| 老人辅助 | AI 辨别药品/物品并提供安全提示   |
| 懒人模式 | 用户一句"帮我看看"，AI自动分析画面 |

---

### 🎯 P2 产品增强能力

* 场景模式切换（教育 / 陪伴 / 通用）
* 成本可视化
* AI服务商切换（OpenAI / Gemini）
* 语音开关控制

---

## 2.2 MVP 实际实现（As-Is）

### ✔ 已实现

* 摄像头采集 + 帧截取
* 语音输入 + 文本输入
* 单帧图像 + 语音融合推理
* 场景 Prompt 模板系统
* AI 多 Provider 架构
* 本地成本统计（Token/次数）

---

### ❌ 未实现（有意识降级）

* 连续视频流分析（成本过高）
* 多轮视觉记忆
* 云端语音识别
* 情绪识别模块
* 长期用户画像系统

---

## 2.3 产品决策说明（关键产品思维）

本产品采用策略：

> **"先构建多模态闭环，再优化实时体验"**

优先保证：

* 能用（MVP闭环）
* 再优化（实时性）
* 最后增强（智能化）

---

# 3. 🏗 系统架构设计（System Design）

---

## 3.1 总体架构（端云协同）

### 🌐 浏览器端（Client Layer）

职责：

* 摄像头采集（Camera.js）
* 语音识别/合成（Speech.js）
* UI交互（App.js）
* 场景管理（Scenes.js）

---

### ☁ AI服务层（Service Layer）

采用抽象 Provider 架构：

* AIProvider（统一接口）
* OpenAIProvider（GPT-4o / mini）
* GeminiProvider（Vision模型）
* MockProvider（离线模式）

---

### 💰 成本控制层（Cost Layer）

* Token统计
* 请求计数
* 费用估算
* 调用频率监控

---

## 3.2 核心交互流程

`
用户语音 / 文本输入
        ↓
App.js（主控制器）
        ↓
Camera.js（帧采集 + 压缩）
        ↓
AIProvider.analyze(image + text + prompt)
        ↓
AI模型返回结果 + cost
        ↓
UI展示 + TTS语音播报
`

---

## 3.3 架构设计原则（产品视角）

* 轻量化优先（Vanilla JS）
* AI能力可插拔（Provider设计）
* 端侧优先处理（降低成本）
* 单次合并推理（降低API调用）

---

# 4. 💰 成本控制与运营策略（核心亮点）

---

## 4.1 成本问题定义

多模态AI的主要成本来自：

* 📸 图像输入 token 成本
* 🎤 语音识别 API
* 🧠 多轮推理成本
* ⚡ 高频实时调用

---

## 4.2 设计阶段优化策略（To-Be）

### 📌 策略1：事件驱动触发（核心创新）

❌ 不做持续视频分析
✅ 只在用户交互时触发 AI

👉 本质：从"流式AI"变为"意图驱动AI"

---

### 📌 策略2：帧采样机制

* 每 N 帧截取一次画面
* 避免逐帧推理

👉 成本下降 60%+

---

### 📌 策略3：图像压缩策略

* JPEG 30%–90%
* 限制 320–640px 分辨率

👉 传输成本下降 90%

---

### 📌 策略4：低成本模型策略

* 优先 gpt-4o-mini
* 使用 detail:low 模式

👉 API成本下降约10倍

---

### 📌 策略5：端侧预处理

* Web Speech API（免费）
* isProcessing防重复调用
* 限流机制

---

## 4.3 实际落地策略（As-Is）

### ✔ 已实现

* 帧采样（非视频流）
* 图像压缩 + 分辨率限制
* 单次合并调用（图+语音）
* 请求频率控制
* 本地语音识别
* 成本统计面板

---

### ❌ 未采用（刻意放弃）

* API缓存（影响实时体验）
* 流式视频分析（成本不可控）
* 云语音API（增加费用）
* 多模型fallback（复杂度过高）

---

## 4.4 成本估算模型

| 使用强度 | 月成本   |
| ---- | ----- |
| 低频用户 | ~\.3 |
| 中频用户 | ~\.8 |
| 高频用户 | ~\.0 |

---

# 5. 🚀 产品创新点（重点加分）

---

## 🌟 创新点1：多模态轻量闭环

传统AI：

> 文本对话

本产品：

> 视觉 + 语音 + 场景理解统一入口

👉 将 AI 从"聊天工具"升级为"环境感知助手"

---

## 🌟 创新点2：事件驱动AI架构

核心突破：

> ❌ 不做持续推理
> ✅ 做用户触发式智能

👉 大幅降低成本 + 提升可控性

---

## 🌟 创新点3：可插拔AI Provider架构

支持：

* OpenAI
* Gemini
* Mock

👉 具备产品级扩展能力

---

## 🌟 创新点4：端云协同设计

* 端侧负责采集 + 预处理
* 云侧负责推理 + 生成

👉 接近工业级AI架构设计

---

# 6. 📊 项目总结（Product Thinking Summary）

本项目遵循：

> **"先闭环验证 → 再优化体验 → 最后提升智能"**

完成：

* 多模态交互 MVP
* 低成本AI架构设计
* 可扩展AI服务框架
* 浏览器端完整AI应用闭环

---

# 7. 🔮 未来演进方向（Roadmap）

* WebRTC实时视频流分析
* 连续视觉记忆（Scene Memory）
* 情绪识别与陪伴模式
* WebLLM端侧AI运行
* 移动端App化
* SaaS API化能力输出
